[tool.poetry]
name = "yolo-live"
version = "0.1.0"
description = "Live object detection with YOLO or OpenCV DNN"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"